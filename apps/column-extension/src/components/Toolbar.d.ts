import { Editor } from "@tiptap/react";
import "./Toolbar.scss";
declare type ToolbarProps = {
    editor: Editor;
};
declare function Toolbar({ editor }: ToolbarProps): JSX.Element;
export { Toolbar };
